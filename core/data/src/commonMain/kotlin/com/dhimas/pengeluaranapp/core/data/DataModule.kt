package com.dhimas.pengeluaranapp.core.data

import org.koin.dsl.module

// Placeholder Koin module for data/repository bindings
val coreDataModule = module {
    // e.g., single<UserRepository> { UserRepositoryImpl(get(), get()) }
}
