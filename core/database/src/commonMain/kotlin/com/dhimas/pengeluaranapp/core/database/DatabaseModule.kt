package com.dhimas.pengeluaranapp.core.database

import org.koin.dsl.module

// Placeholder Koin module for database bindings
val coreDatabaseModule = module {
    // e.g., single<LocalUserDataSource> { AndroidRoomLocalUserDataSource(get()) } on Android later
}
